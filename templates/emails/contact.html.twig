<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email from {{ contact.email}}</title>
</head>
<body>
    <main>
        <div class="content">
            <h1>Contact Request form {{ contact.email }}</h1>
            <table class="tech-table" border="0">
                <tr>
                    <td>First name</td>
                    <td class="font-semibold">{{ contact.firstname }}</td>
                </tr>
                <tr>
                    <td>Last name</td>
                    <td class="font-semibold">{{ contact.firstname }}</td>
                </tr>
                <tr>
                    <td>Email</td>
                    <td class="font-semibold">{{ contact.email }}</td>
                </tr>
                <tr>
                    <td>Phone</td>
                    <td class="font-semibold">{{ contact.phone }}</td>
                </tr>
            </table>

            <div class="message">
                <p>{{ contact.message | nl2br }}</p>
            </div>
        </div>
        

    </main>
</body>
</html>